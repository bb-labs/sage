app.bump.patch.semver.bump_patch: ${APP_IMAGE_TAG}
app.bump.patch.read_text: ${ENV_PATH}
app.bump.patch.re.sub:
  pattern: "APP_IMAGE_TAG=(.*)"
  repl: 'APP_IMAGE_TAG="$(app.bump.patch.semver.bump_patch)"'
  string: $(app.bump.patch.read_text)
app.bump.patch.write_text:
  path: ${ENV_PATH}
  data: $(app.bump.patch.re.sub)
---
app.bump.minor.semver.bump_minor: ${APP_IMAGE_TAG}
app.bump.minor.read_text: ${ENV_PATH}
app.bump.minor.re.sub:
  pattern: "APP_IMAGE_TAG=(.*)"
  repl: 'APP_IMAGE_TAG="$(app.bump.minor.semver.bump_minor)"'
  string: $(app.bump.minor.read_text)
app.bump.minor.write_text:
  path: ${ENV_PATH}
  data: $(app.bump.minor.re.sub)
---
app.bump.major.semver.bump_major: ${APP_IMAGE_TAG}
app.bump.major.read_text: ${ENV_PATH}
app.bump.major.re.sub:
  pattern: "APP_IMAGE_TAG=(.*)"
  repl: 'APP_IMAGE_TAG="$(app.bump.major.semver.bump_major)"'
  string: $(app.bump.major.read_text)
app.bump.major.write_text:
  path: ${ENV_PATH}
  data: $(app.bump.major.re.sub)
