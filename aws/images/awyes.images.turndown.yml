images.turndown.secrets.ecr.list_images:
  repositoryName: ${SECRETS_ECR_REPO_NAME}
images.turndown.secrets.ecr.batch_delete_image:
  repositoryName: ${SECRETS_ECR_REPO_NAME}
  imageIds: $(images.turndown.secrets.ecr.list_images.imageIds)
---
images.turndown.app.ecr.list_images:
  repositoryName: ${APP_ECR_REPO_NAME}
images.turndown.app.ecr.batch_delete_image:
  repositoryName: ${APP_ECR_REPO_NAME}
  imageIds: $(images.turndown.app.ecr.list_images.imageIds)
