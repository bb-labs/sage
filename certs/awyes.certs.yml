certs.acm.request_certificate:
  DomainName: ${SAGE_DOMAIN}
  ValidationMethod: DNS
  SubjectAlternativeNames:
    - ${WSS_DOMAIN}
    - ${APP_DOMAIN}
certs.acm.describe_certificate:
  CertificateArn: $(certs.acm.request_certificate.CertificateArn)