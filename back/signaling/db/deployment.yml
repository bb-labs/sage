apiVersion: v1
kind: Secret
metadata:
  name: signaling-db-secret
type: Opaque
data:
  username: dXNlcm5hbWU=
  password: cGFzc3dvcmQ=

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: signaling-db
  labels:
    app: signaling-db
spec:
  replicas: 1
  selector:
    matchLabels:
      app: signaling-db
  template:
    metadata:
      labels:
        app: signaling-db
    spec:
      containers:
        - name: signaling-db
          image: mongo
          ports:
            - containerPort: 27017
          env:
            - name: MONGO_INITDB_ROOT_USERNAME
              valueFrom:
                secretKeyRef:
                  name: signaling-db-secret
                  key: username
            - name: MONGO_INITDB_ROOT_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: signaling-db-secret
                  key: password

---
apiVersion: v1
kind: Service
metadata:
  name: signaling-db-service
spec:
  selector:
    app: signaling-db
  ports:
    - protocol: TCP
      port: 4001
      targetPort: 27017

---

