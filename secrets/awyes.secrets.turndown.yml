secrets.turndown.db.user.ssm.delete_parameter:
  Name: DB_USERNAME
secrets.turndown.db.pass.ssm.delete_parameter:
  Name: DB_PASSWORD
secrets.turndown.apple.secrets.delete_secret:
  SecretId: APPLE_CLIENT_SECRET
  ForceDeleteWithoutRecovery: true
secrets.turndown.apple.lmda.delete_function:
  FunctionName: ${SECRETS_LAMBDA_NAME}
secrets.turndown.apple.ecr.list_images:
  repositoryName: ${SECRETS_ECR_REPO_NAME}
secrets.turndown.apple.ecr.batch_delete_image:
  repositoryName: ${SECRETS_ECR_REPO_NAME}
  imageIds: $(secrets.turndown.apple.ecr.list_images.imageIds)
secrets.turndown.apple.ecr.delete_repository:
  repositoryName: ${SECRETS_ECR_REPO_NAME}
